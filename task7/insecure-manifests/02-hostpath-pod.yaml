apiVersion: v1
kind: Pod
metadata:
  name: insecure-hostpath
  namespace: audit-zone
spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - name: host-root
      mountPath: /host
  volumes:
  - name: host-root
    hostPath:
      path: /
      type: Directory
